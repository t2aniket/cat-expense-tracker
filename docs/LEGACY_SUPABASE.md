# Legacy Supabase Notes

This project originally shipped with Supabase setup instructions. It now uses Vercel/Neon Postgres instead so deployment does not require copying Supabase keys or running SQL manually.

The old SQL file has been removed because the Postgres service now creates the required tables automatically.
